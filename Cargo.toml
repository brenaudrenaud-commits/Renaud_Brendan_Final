[package]
name = "final_project"
version = "0.1.0"
edition = "2024"

[dependencies]
rusqlite = { version = "0.31", features = ["bundled"] }
axum = "0.8.8"
tokio = {version= "1", features = ["full"]}
serde = {version = "1", features = ["derive"]}
serde_json = "1.0.68"